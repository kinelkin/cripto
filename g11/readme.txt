Readme:

Para compilar el codigo ejecutar el comando "make all"
Para eliminar la basura (.o y .txts etc) ejecutar "make clean"

Para probar vigenere, decypher, permutation hay 3 sh:
  vigenere.sh
  decypher.sh
  perm.sh

  Se pueden ejecutar : sh vigenere.sh que realiza varios experimentos
